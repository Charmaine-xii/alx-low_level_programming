#include "main.h"
#include <unistd.h>

/**
* _putchar -writes the charcter c to standout
* @c: the character to print
*
* Return: successful 1, on error -1
*/
int _putchar (char c)
{
	return(write(1, &C, 1);
}
